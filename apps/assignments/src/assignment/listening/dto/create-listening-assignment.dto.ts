import { CreateObjectiveAssignmentBaseDto } from '../../dto/objective/objective-assignment.dto';

/** Listening assignment create body (material.type must be `listening` per section). */
export class CreateListeningAssignmentDto extends CreateObjectiveAssignmentBaseDto {}
