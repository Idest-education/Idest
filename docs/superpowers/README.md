# Agent working files

`plans/` holds transient implementation plans generated by agent tooling
(git-ignored). Authoritative design docs live in `docs/specs/`.

For the full map of repo documentation, see `docs/manifest.md`.
